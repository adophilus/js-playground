# On NixOS use `shell.nix` instead (pip wheels lack the system shared libs).
# This file documents the same deps for non-Nix machines:
opencv-python-headless>=4.10
numpy>=1.26
Pillow>=10.0
pytesseract>=0.3.10
